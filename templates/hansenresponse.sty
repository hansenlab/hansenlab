\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{hansenresponse}
\RequirePackage{palatino}
\RequirePackage{mathpazo}
\RequirePackage[english]{babel}
\RequirePackage[utf8]{inputenc}
\RequirePackage{parskip}
\RequirePackage{fullpage}
\RequirePackage{doi}
\RequirePackage{url,hyperref,xcolor}
\definecolor{darkblue}{rgb}{0.0,0.0,0.75}
\hypersetup{colorlinks,breaklinks,
            linkcolor=darkblue,urlcolor=darkblue,
            anchorcolor=darkblue,citecolor=darkblue}
\RequirePackage{mathtools}
\RequirePackage{graphicx}
\RequirePackage{subfig}
\RequirePackage[labelfont=bf,labelsep=period,justification=raggedright]{caption}
\newenvironment{response}{\begingroup\textbf{Response:}}{\endgroup}
\renewenvironment{quote}{\list{}{\rightmargin\leftmargin}%
\item\relax\textquotedblleft\ignorespaces\color{darkblue}}
{\unskip\unskip\textquotedblright\endlist}
\newenvironment{paperquote}{\begingroup\relax\textquotedblleft\ignorespaces\unskip\unskip\it}{\unskip\unskip\textquotedblright\endgroup}

\newcommand{\fixme}[1]{{\color{red} FIXME: #1}}
\newcommand{\XX}{{\color{red} FIXME: XX}}

%% Citations
\RequirePackage[autostyle]{csquotes}
\RequirePackage[backend=biber,style=authoryear,sorting=nyt,natbib=true,
         maxbibnames=99,giveninits=true,uniquename=false]{biblatex}
\renewbibmacro{in:}{}

%% Comments in code
%\newcommand{\fixme}[1]{{\color{red} FIXME: #1}}
%\newcommand{\status}[1]{{\color{red} STATUS: #1}}
%\newcommand{\XX}{{\color{red} FIXME: XX}}


\endinput
%%
%% End of file `hansenresponse.sty'.

